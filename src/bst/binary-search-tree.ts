/**
 * At most 2 child nodes, left and right.
 * Left child will always have a lower value compared to parent.
 * Right child will always have a higher value compared to parent.
 * Same rules apply for subtrees.
 */
export class BinarySearchTree<T> {
  constructor() {}
}
